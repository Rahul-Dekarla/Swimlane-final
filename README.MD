Information about how you set up the Kubernetes cluster” → docs/kubernetes-cluster-setup.md

commands used to deploy application and Terraform” → docs/commands.md

“Helm charts used to deploy the application” → swimlane

“Terraform files” → terraform/

“Dockerfiles” → devops-practical/Dockerfile

“Screenshot of running application with a new record” → docs/screenshots/app-running-new-record.png
